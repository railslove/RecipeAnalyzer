<%= form_for(@ingredient_name) do |f| %>
  <% if @ingredient_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ingredient_name.errors.count, "error") %> prohibited this ingredient_name from being saved:</h2>

      <ul>
      <% @ingredient_name.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :locale %><br />
    <%= f.text_field :locale %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
